
// The source program with error

//wrong header file
# include <std.h>
#include<math.h>

int main(){
    // wrong variable name
    int 123xyz;

    // if statement with wrong conditional statement
    if(x<10;){
        x = 10;
    }
    char ch;
    else{ // wrong else statement (extra statement between if and else)
        x = 2*x;
    }
    // for loop with missing Closing_Parenthesis
    for(i=0; i<10; i++ {
        int temp;
    }
    // for loop with wrong statement ( require 3 statements)
    for( i =0 ; i<10 ){
        i++;
    }
    return 0;
}
